package main

import (
	"fmt"
)

func main() {
	testMap := make(map[string]string)
	testMap["France"] = "Paris"
	slice := make([]int, 10)
	slice = [] int{1, 2, 3}
	fmt.Println(slice[:])
	fmt.Println("hello");
}